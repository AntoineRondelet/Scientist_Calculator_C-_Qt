/*#ifndef IdentificateurTable_H
#define IdentificateurTable_H

#include <QMap>
#include "operateur.h"

// NE PAS laisser l'utilisateur modifier cette table !!! /!\ Elle ne doit etre modifiée que lors de la création d'un opérateur !!
class IdentificateurTable {
    friend class Operateur;
    QMap<QString, QString> mapIdentificateurs;
public:
    IdentificateurTable(): mapIdentificateurs(QMap<QString, QString>()){}
};

#endif // IdentificateurTable_H
*/
